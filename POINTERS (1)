#include<stdio.h>
int main(){
    int *x,*y,a,b;
    printf("enter the value of a and b\n");
    scanf("%d%d",&a,&b);
    x=&a;
    y=&b;
    int SUM=*x+*y;
    printf("SUM of a and b is: %d",SUM);
}
