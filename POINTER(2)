#include<stdio.h>
void add(int *x,int *y)
{
    int SUM=*x+*y;
    printf("SUM=%d",SUM);
}
int main()
{
    int a,b;
    printf("enter the values of a and b\n");
    scanf("%d %d",&a,&b);
    add(&a,&b);
    
}

